# GitHub Pagesへの反映

1. ZIPを展開します。
2. リポジトリ直下へ以下をアップロードします。
   - `index.html`
   - `assets` フォルダ
3. `index.html` と `assets` が同じ階層にあることを確認します。
4. GitHubの `Settings → Pages` で公開元を `main / (root)` にします。
5. 公開後、古い表示が残る場合は `Ctrl + F5` で再読み込みします。

この版は外部ライブラリ、サーバー、データベースを使用しません。
